# 🚀 Финальная инструкция по деплою на GitHub Pages

## ✅ ВСЕ ПУТИ ИСПРАВЛЕНЫ!

Все пути обновлены с `/u1179532223/5339356/` на `/UCB-SIte-CRYPTO/` для работы с GitHub Pages как проект-страница.

---

## ⚠️ КРИТИЧЕСКИ ВАЖНО: Структура файлов на GitHub

**Файлы должны быть в КОРНЕ репозитория на GitHub, а не в подпапке!**

### Текущая локальная структура:
```
UCB-SIte-CRYPTO/
└── Readymag - UCB/
    └── u1179532223/
        └── 5339356/
            ├── index.html
            ├── dist/
            ├── img/
            └── ...
```

### Должна быть на GitHub:
```
UCB-SIte-CRYPTO/  (корень репозитория)
├── index.html
├── 404.html
├── .nojekyll
├── dist/
│   ├── viewer.js
│   ├── viewer.css
│   ├── config.js
│   └── c/
├── img/
├── snippets/
├── server-data.json
├── sw.js
├── robots.txt
└── sitemap.xml
```

---

## 📋 Что было исправлено:

### 1. ✅ `index.html`
- `base href="/UCB-SIte-CRYPTO/"`
- `publicPath: "/UCB-SIte-CRYPTO/dist"`
- `exportBasePath: "/UCB-SIte-CRYPTO"`
- Все ссылки на ресурсы используют `/UCB-SIte-CRYPTO/`

### 2. ✅ `dist/config.js`
- `root: "/UCB-SIte-CRYPTO/"`
- `chunkURL: "/UCB-SIte-CRYPTO/dist/"`

### 3. ✅ `sw.js`
- `BASE_PATH = '/UCB-SIte-CRYPTO'`

### 4. ✅ `404.html`
- Создан с правильным путем `/UCB-SIte-CRYPTO/index.html`

### 5. ✅ Все файлы в `dist/c/` (224 файла)
- Все импорты обновлены на `/UCB-SIte-CRYPTO/dist/c/`

### 6. ✅ `dist/viewer.js`
- Все импорты обновлены на `/UCB-SIte-CRYPTO/dist/c/`

### 7. ✅ `.nojekyll`
- Создан для отключения Jekyll

---

## 🔧 Инструкция по копированию файлов на GitHub

### Вариант 1: Через GitHub Web Interface

1. Откройте репозиторий на GitHub
2. Убедитесь, что вы в корне репозитория (не в подпапках)
3. Загрузите файлы из локальной папки `Readymag - UCB\u1179532223\5339356\` в корень репозитория

**Важно**: Все файлы должны быть на одном уровне с `.git`, `.gitignore` и т.д.

### Вариант 2: Скопировать файлы локально и затем закоммитить

Если файлы должны быть в корне репозитория, но сейчас они в подпапке:

```powershell
# Перейти в корень репозитория
cd C:\Users\Tigran\Documents\GitHub\UCB-SIte-CRYPTO

# Скопировать все файлы из подпапки в корень
Copy-Item "Readymag - UCB\u1179532223\5339356\*" -Destination . -Recurse -Force

# Проверить, что файлы скопированы
ls index.html, 404.html, .nojekyll
```

Затем закоммитить и запушить.

---

## 📝 Проверка перед деплоем

Проверьте, что в корне репозитория на GitHub есть:

- [ ] `index.html` (в корне, не в подпапке)
- [ ] `404.html` (в корне)
- [ ] `.nojekyll` (в корне)
- [ ] `dist/` (папка в корне)
- [ ] `img/` (папка в корне)
- [ ] `snippets/` (папка в корне)
- [ ] `server-data.json` (в корне)
- [ ] `sw.js` (в корне)
- [ ] `robots.txt` (в корне)
- [ ] `sitemap.xml` (в корне)

---

## ⚙️ Настройка GitHub Pages

1. Откройте репозиторий на GitHub
2. Перейдите в **Settings** → **Pages**
3. В разделе **Source**:
   - **Branch**: выберите `main` (или `master`)
   - **Folder**: выберите `/ (root)`
4. Нажмите **Save**

---

## 🔍 После деплоя проверьте:

1. **URL сайта**: `https://username.github.io/UCB-SIte-CRYPTO/`
2. **Консоль браузера**: не должно быть ошибок 404
3. **Все ресурсы загружаются**: проверьте Network tab в DevTools
4. **Навигация работает**: переходы между страницами

---

## 🐛 Если все еще есть ошибки 404:

1. **Проверьте структуру на GitHub**: файлы должны быть в корне, не в подпапке
2. **Проверьте, что файлы закоммичены**: все файлы должны быть в репозитории
3. **Очистите кэш браузера**: Ctrl+Shift+Delete
4. **Проверьте настройки GitHub Pages**: должны быть настроены на `/ (root)`

---

## 📌 Важные заметки:

- Все пути теперь используют `/UCB-SIte-CRYPTO/` как префикс
- Это необходимо для работы GitHub Pages как проект-страницы
- Если вы хотите использовать кастомный домен (unitedcryptoboys.online), пути останутся теми же
- После настройки кастомного домена GitHub Pages автоматически настроит правильные пути

---

**Готово к деплою!** 🎉

Все пути исправлены. Убедитесь, что файлы находятся в корне репозитория на GitHub.
