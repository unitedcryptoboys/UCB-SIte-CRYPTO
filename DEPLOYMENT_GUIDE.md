# 🚀 Руководство по деплою на GitHub Pages

## Подготовка к деплою

Все необходимые файлы уже готовы:
- ✅ `index.html` - главная страница
- ✅ `404.html` - редирект для SPA routing
- ✅ `.nojekyll` - отключение Jekyll
- ✅ `.gitignore` - исключение ненужных файлов
- ✅ Все пути исправлены на `/`

---

## 📋 Пошаговая инструкция

### Шаг 1: Проверка Git статуса

Убедитесь, что вы в корневой директории проекта:

```bash
cd C:\Users\Tigran\Documents\GitHub\UCB-SIte-CRYPTO
```

Проверьте статус Git:

```bash
git status
```

### Шаг 2: Добавление файлов

Добавьте все файлы в staging area:

```bash
git add .
```

Или добавьте файлы по отдельности:

```bash
git add index.html
git add 404.html
git add .nojekyll
git add .gitignore
git add dist/
git add img/
git add snippets/
git add server-data.json
git add robots.txt
git add sitemap.xml
git add sw.js
git add README.md
```

**Важно**: Не добавляйте служебные файлы конфигурации для других серверов:
- `.htaccess` (для Apache - не используется на GitHub Pages)
- `web.config` (для IIS - не используется)
- `nginx_rmwebsite.conf` (для Nginx - не используется)

Эти файлы можно добавить в `.gitignore` или оставить как есть (они не мешают работе).

### Шаг 3: Коммит изменений

Создайте коммит с описанием:

```bash
git commit -m "Prepare project for GitHub Pages deployment

- Fixed all resource paths from /u1179532223/5339356/ to /
- Added 404.html for SPA routing
- Added .nojekyll to disable Jekyll
- Added .gitignore
- All files ready for deployment"
```

### Шаг 4: Проверка ветки

Убедитесь, что вы на правильной ветке (обычно `main` или `master`):

```bash
git branch
```

Если нужно переключиться:

```bash
git checkout main
```

или

```bash
git checkout master
```

### Шаг 5: Настройка remote (если еще не настроен)

Если репозиторий еще не подключен к GitHub:

```bash
git remote add origin https://github.com/unitedcryptoboys/UCB-SIte-CRYPTO.git
```

Проверьте remote:

```bash
git remote -v
```

### Шаг 6: Push на GitHub

Отправьте изменения на GitHub:

```bash
git push -u origin main
```

или, если ваша ветка называется `master`:

```bash
git push -u origin master
```

### Шаг 7: Настройка GitHub Pages

1. Откройте репозиторий на GitHub: https://github.com/unitedcryptoboys/UCB-SIte-CRYPTO
2. Перейдите в **Settings** (Настройки)
3. В левом меню найдите **Pages**
4. В разделе **Source** выберите:
   - **Branch**: `main` (или `master`)
   - **Folder**: `/ (root)`
5. Нажмите **Save**

### Шаг 8: Ожидание деплоя

GitHub Pages автоматически начнет деплой. Это может занять 1-5 минут.

Статус деплоя можно отслеживать:
- В вкладке **Actions** репозитория
- В разделе **Settings → Pages** (будет показан статус)

### Шаг 9: Проверка сайта

После завершения деплоя сайт будет доступен по адресу:
- `https://unitedcryptoboys.github.io/UCB-SIte-CRYPTO/`

Если настроен кастомный домен:
- `https://unitedcryptoboys.online`

---

## ⚙️ Настройка кастомного домена (опционально)

Если вы хотите использовать домен `unitedcryptoboys.online`:

### В GitHub:

1. В **Settings → Pages → Custom domain** введите:
   ```
   unitedcryptoboys.online
   ```
2. Отметьте **Enforce HTTPS**
3. Сохраните

### Настройка DNS:

У вашего регистратора домена добавьте DNS записи:

**A записи** (IPv4):
```
185.199.108.153
185.199.109.153
185.199.110.153
185.199.111.153
```

**CNAME запись** (для поддомена www, если нужен):
```
www → unitedcryptoboys.github.io
```

**Или вместо A записей можно использовать CNAME**:
```
@ → unitedcryptoboys.github.io
```

### Проверка DNS:

Дождитесь распространения DNS (может занять до 48 часов). Проверить можно:
- https://www.whatsmydns.net/
- https://dnschecker.org/

---

## ✅ Чеклист готовности

Перед деплоем убедитесь:

- [x] Все пути исправлены (`/` вместо `/u1179532223/5339356/`)
- [x] Файл `404.html` присутствует
- [x] Файл `.nojekyll` присутствует
- [x] Файл `.gitignore` создан
- [ ] Все изменения закоммичены
- [ ] Изменения запушены на GitHub
- [ ] GitHub Pages включен в настройках
- [ ] Домен настроен (если используется кастомный)

---

## 🔍 Проверка после деплоя

После успешного деплоя проверьте:

1. **Открытие главной страницы**: сайт должен загружаться без ошибок
2. **Консоль браузера**: не должно быть ошибок 404 для ресурсов
3. **Навигация**: переходы между страницами должны работать
4. **Мобильная версия**: проверьте на мобильном устройстве
5. **Service Worker**: должен регистрироваться (проверьте в DevTools → Application → Service Workers)

---

## 🐛 Решение проблем

### Проблема: Сайт не загружается

**Решения**:
- Проверьте, что выбран правильный branch в настройках Pages
- Убедитесь, что `index.html` находится в корне
- Проверьте логи в **Actions**

### Проблема: 404 ошибки для ресурсов

**Решения**:
- Проверьте, что все пути используют `/` (не `/u1179532223/5339356/`)
- Убедитесь, что файлы `dist/viewer.js`, `dist/viewer.css` присутствуют
- Проверьте консоль браузера для конкретных путей

### Проблема: SPA routing не работает

**Решения**:
- Убедитесь, что `404.html` присутствует в корне
- Проверьте, что `.nojekyll` файл создан

### Проблема: Сайт показывает старую версию

**Решения**:
- Очистите кэш браузера (Ctrl+Shift+Delete)
- Проверьте, что последний коммит запушен
- Дождитесь обновления GitHub Pages (может занять несколько минут)

---

## 📞 Дополнительная информация

- **Документация GitHub Pages**: https://docs.github.com/en/pages
- **Статус деплоя**: Проверяйте в разделе **Actions** репозитория
- **Логи**: В **Actions** можно посмотреть детальные логи деплоя

---

**Готово к деплою!** 🎉
