# ✅ Оптимизация SVG файлов - Настройка завершена

## 📋 Что было сделано

### 1. Добавлены зависимости в package.json ✅
```json
"imagemin": "^8.0.1",
"imagemin-svgo": "^10.0.1"
```

### 2. Создан скрипт оптимизации ✅
- `optimize-svg.js` - автоматически находит и оптимизирует все SVG файлы
- Ищет в: `img/`, `dist/`, `snippets/`, корне проекта
- Применяет агрессивные оптимизации SVGO

### 3. Обновлен package.json ✅
- Добавлен скрипт: `"optimize-svg": "node optimize-svg.js"`
- Интегрирован в `build`: `"build": "npm run optimize && npm run optimize-svg"`

### 4. Обновлен GitHub Actions ✅
- Добавлен шаг оптимизации SVG в `.github/workflows/pagespeed-optimize.yml`
- Автоматически запускается при каждом push

## 🚀 Как использовать

### Автоматически (рекомендуется):
При следующем push на GitHub все SVG файлы будут автоматически оптимизированы.

### Вручную (требует Node.js):
```bash
npm install
npm run optimize-svg
```

## 📊 Что оптимизируется

- ✅ Удаление комментариев и метаданных
- ✅ Минификация стилей
- ✅ Оптимизация путей и трансформаций
- ✅ Объединение групп и путей
- ✅ Округление чисел до 2 знаков
- ✅ Удаление неиспользуемых элементов
- ✅ Сохранение viewBox и title (для accessibility)

## ⚠️ Важно

- Оригинальные SVG файлы заменяются оптимизированными версиями
- Рекомендуется сделать git commit перед первым запуском
- Ожидаемое уменьшение размера: 20-50%

## 📝 Следующие шаги

1. Запустить `npm install` для установки зависимостей
2. Запустить `npm run optimize-svg` для оптимизации (или дождаться автоматического запуска через GitHub Actions)
3. Проверить результаты оптимизации
