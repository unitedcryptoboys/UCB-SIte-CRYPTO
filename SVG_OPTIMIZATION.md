# 🎨 Оптимизация SVG файлов

## ✅ Настройка завершена

### 1. Установлены зависимости
- `imagemin` - инструмент для оптимизации изображений
- `imagemin-svgo` - плагин SVGO для оптимизации SVG

### 2. Создан скрипт оптимизации
- `optimize-svg.js` - автоматическая оптимизация всех SVG файлов
- Ищет SVG в: `img/`, `dist/`, `snippets/`, корне проекта
- Применяет оптимизации:
  - Удаление комментариев и метаданных
  - Минификация стилей
  - Оптимизация путей и трансформаций
  - Объединение групп и путей
  - Округление чисел до 2 знаков
  - Удаление неиспользуемых элементов

### 3. Обновлен package.json
- Добавлен скрипт `optimize-svg`
- Интегрирован в `build` скрипт
- Добавлены зависимости в `devDependencies`

### 4. Обновлен GitHub Actions
- Автоматическая оптимизация SVG при каждом push
- Продолжает работу даже при ошибках (`continue-on-error: true`)

## 🚀 Использование

### Локально (требует Node.js):
```bash
npm install
npm run optimize-svg
```

### Автоматически:
При следующем push на GitHub автоматически оптимизируются все SVG файлы.

## 📊 Ожидаемые результаты

SVG файлы обычно уменьшаются на **20-50%** после оптимизации:
- Удаление неиспользуемых элементов
- Минификация путей и стилей
- Оптимизация числовых значений
- Удаление комментариев и метаданных

## ⚠️ Примечания

- Скрипт сохраняет `viewBox` и `title` для правильной работы и accessibility
- Оригинальные файлы заменяются оптимизированными версиями
- Рекомендуется делать бэкап перед первым запуском
